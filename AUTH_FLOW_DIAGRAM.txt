```
╔══════════════════════════════════════════════════════════════════════════╗
║                AWS COGNITO AUTHENTICATION FLOW                            ║
║                        CloudSpace Application                             ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                          1. USER SIGNUP FLOW                             │
└─────────────────────────────────────────────────────────────────────────┘

    User                  CloudSpace App              AWS Cognito
     │                         │                           │
     ├──────signup form────────>│                           │
     │   (name, email, pwd)     │                           │
     │                          │                           │
     │                          ├────signUp() API──────────>│
     │                          │                           │
     │                          │<───returns userId─────────┤
     │                          │   nextStep: CONFIRM       │
     │                          │                           │
     │<─verification code form──┤                           │
     │                          │                           │
     │                          │<──email with code─────────┤
     │                          │                           │
     ├──enters 6-digit code────>│                           │
     │                          │                           │
     │                          ├─confirmSignUp() API──────>│
     │                          │                           │
     │                          │<──account confirmed───────┤
     │                          │                           │
     │<──redirect to /login─────┤                           │
     │                          │                           │


┌─────────────────────────────────────────────────────────────────────────┐
│                          2. USER LOGIN FLOW                              │
└─────────────────────────────────────────────────────────────────────────┘

    User                  CloudSpace App              AWS Cognito
     │                         │                           │
     ├─────login form──────────>│                           │
     │   (email, password)      │                           │
     │                          │                           │
     │                          ├─────signIn() API─────────>│
     │                          │  (Authorization Code +    │
     │                          │   PKCE flow)              │
     │                          │                           │
     │                          │<──JWT Tokens──────────────┤
     │                          │  (idToken, accessToken)   │
     │                          │                           │
     │                          ├─store in localStorage─────┐
     │                          │ "cloudspace_token"        │
     │                          │<──────────────────────────┘
     │                          │                           │
     │<───redirect to /─────────┤                           │
     │    (Dashboard)           │                           │


┌─────────────────────────────────────────────────────────────────────────┐
│                    3. ACCESSING PROTECTED ROUTES                         │
└─────────────────────────────────────────────────────────────────────────┘

    User                  CloudSpace App              AWS Cognito
     │                         │                           │
     ├──access /dashboard──────>│                           │
     │                          │                           │
     │                          ├─ProtectedRoute checks────┐
     │                          │ token exists?             │
     │                          │<──────────────────────────┘
     │                          │                           │
     │                          ├─fetchAuthSession()───────>│
     │                          │                           │
     │                          │<─validate token───────────┤
     │                          │  (returns user session)   │
     │                          │                           │
     │<────show dashboard───────┤                           │
     │                          │                           │
     │                          │                           │
     │  IF TOKEN INVALID:       │                           │
     │<──redirect to /login─────┤                           │


┌─────────────────────────────────────────────────────────────────────────┐
│                          4. GET CURRENT USER                             │
└─────────────────────────────────────────────────────────────────────────┘

    Component            authUtils.js                 AWS Cognito
     │                         │                           │
     ├──getCurrentUser()───────>│                           │
     │                          │                           │
     │                          ├─getCurrentUser() API─────>│
     │                          │                           │
     │                          │<──user attributes─────────┤
     │                          │                           │
     │                          ├─fetchAuthSession()───────>│
     │                          │                           │
     │                          │<──JWT tokens──────────────┤
     │                          │                           │
     │<──returns user object────┤                           │
     │   { userId, email,       │                           │
     │     name, tokens }       │                           │


┌─────────────────────────────────────────────────────────────────────────┐
│                          5. LOGOUT FLOW                                  │
└─────────────────────────────────────────────────────────────────────────┘

    User                  CloudSpace App              AWS Cognito
     │                         │                           │
     ├─clicks logout───────────>│                           │
     │                          │                           │
     │                          ├──signOut() API───────────>│
     │                          │                           │
     │                          │<──session ended───────────┤
     │                          │                           │
     │                          ├─clear localStorage────────┐
     │                          │ remove "cloudspace_token" │
     │                          │<──────────────────────────┘
     │                          │                           │
     │<──redirect to /login─────┤                           │


╔══════════════════════════════════════════════════════════════════════════╗
║                         KEY COMPONENTS                                    ║
╠══════════════════════════════════════════════════════════════════════════╣
║                                                                           ║
║  📁 /auth/Login.jsx            - Login page with AWS Cognito signIn()    ║
║  📁 /auth/Signup.jsx           - Signup with email verification          ║
║  📁 /auth/ProtectedRoute.jsx   - Guards protected routes                 ║
║  📁 /auth/authUtils.js         - Helper functions                        ║
║  📁 /aws-config.js             - AWS Amplify configuration               ║
║  📁 /components/Navbar.jsx     - User info & logout                      ║
║  📁 /App.jsx                   - Main routing                            ║
║                                                                           ║
╠══════════════════════════════════════════════════════════════════════════╣
║                       SECURITY FEATURES                                   ║
╠══════════════════════════════════════════════════════════════════════════╣
║                                                                           ║
║  ✓ Authorization Code + PKCE   - No client secret needed                 ║
║  ✓ JWT Tokens                  - Secure authentication                   ║
║  ✓ Email Verification          - Required for signup                     ║
║  ✓ Strong Passwords            - Enforced by Cognito                     ║
║  ✓ Token Auto-Refresh          - Handled by Amplify                      ║
║  ✓ Route Protection            - Unauthorized redirect to login          ║
║                                                                           ║
╚══════════════════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════════════════╗
║                      TOKEN STORAGE STRUCTURE                              ║
╚══════════════════════════════════════════════════════════════════════════╝

    localStorage
    └── "cloudspace_token" → JWT ID Token
        ├── Header (algorithm, type)
        ├── Payload
        │   ├── sub (user ID)
        │   ├── email
        │   ├── name
        │   ├── email_verified
        │   ├── iat (issued at)
        │   ├── exp (expiration)
        │   └── aud (audience)
        └── Signature (verified by AWS)

╔══════════════════════════════════════════════════════════════════════════╗
║                           API FUNCTIONS                                   ║
╚══════════════════════════════════════════════════════════════════════════╝

    getCurrentUser()      → Returns full user object with tokens
    getToken()           → Returns JWT from localStorage
    isAuthenticated()    → Boolean check for auth status
    signOutUser()        → Signs out and clears token


═══════════════════════════════════════════════════════════════════════════
                          AUTHENTICATION READY! ✅
═══════════════════════════════════════════════════════════════════════════
```
